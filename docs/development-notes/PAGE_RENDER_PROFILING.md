# Page Render Profiling

This document is superseded by `PAGE_RENDER_PROFILING_LITE.md` in v28.

v28 intentionally removes the very verbose v27 per-word/per-glyph profiling because serial output at 115200 baud can itself slow rendering and make touch input feel unresponsive.

Use `PAGE_RENDER_PROFILING_LITE.md` for the current log format.
