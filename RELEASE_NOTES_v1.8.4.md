# PaperPoint S3 Reader v1.8.4 Release Notes

發布日期：2026-07-16
硬體：M5Stack Paper S3 / M5Paper S3
內部修訂整合：r19–r34i

## 發布重點

### 電量顯示

- 改用 Paper S3 USB 偵測腳位判斷外部供電，不再用 USB Serial 是否連線判斷。
- 電池 ADC 採多次取樣與中位數／平滑追蹤。
- USB 插入或拔除後先等待端電壓穩定，再逐步更新顯示百分比，避免插拔瞬間由高百分比跳到低百分比。

### BMI270 校正、閱讀方向與感應上鎖

- 控制選單新增四姿勢校正：正常直立、頂端朝下 180°、螢幕朝上、螢幕朝下。
- 未完成有效校正前，不啟用自動方向偵測或感應上鎖。
- 閱讀方向提供固定正向、固定 180°、自動 0°/180°，不提供 90°。
- 閱讀內容、背景 PNG、狀態列、閱讀／漫畫選單、章節畫面、底部按鈕及觸控座標同步旋轉。
- 感應上鎖只可搭配固定方向；自動方向模式會強制關閉感應上鎖。
- 方向切換會使 EPUB 背景、頁面 framebuffer cache 與 CBZ 預載 cache 失效，避免切回正向仍顯示倒向背景。

### EPUB / TXT 閱讀

- 新增段落首行空兩個字，橫排、直排及 TXT 分頁均納入計算。
- 閱讀背景 PNG 支援選檔、淡化與 PSRAM framebuffer cache。
- 文字輔助線支援實線、虛線、點線，並依實際文字行列位置繪製。
- 黑白反轉會同步套用閱讀背景與輔助線。
- 狀態列跟隨頁邊距時，正文會保留額外安全間距。
- 翻頁不再等待背景 frame cache 完成；cache 未完成時直接 visible render，保留一個 pending 翻頁。

### CBZ / ZIP 漫畫

- 新增漫畫專用檔案瀏覽與閱讀器，支援 ZIP 中 JPG/JPEG/PNG/BMP，自然排序並記錄進度。
- 下一頁可在背景預載；預載期間收到的翻頁會排入 pending，在工作完成後立即執行。
- 漫畫選單觸控區與畫面列對齊，單次點擊即可執行。
- 移除四階灰階選項；新增每頁／每 2 頁／每 5 頁／每 10 頁／不全刷。
- 改善 ZIP 解壓與圖片解碼 watchdog cooperative yield。

### 休眠圖片與關機

- 自訂休眠圖片支援 `/.sleep`、`/cover` 與相容 `/sleep`，可瀏覽多層資料夾。
- 可選擇隨機或固定圖片；第二次選取進入預覽，提供取消與確認。
- 不透明圖片中央裁切填滿；透明 PNG 保持比例並與閱讀頁或白底融合。
- 裝置閒置時預先建立睡眠圖片快取，關機時優先使用已完成或上一張有效快取。
- 閱讀頁左上角保留 64×64 隱藏關機熱區，不顯示圖示且優先於翻頁區。

### 檔案瀏覽與操作

- 非根目錄第一列固定顯示 `..`。
- 短按返回：回上一層；位於 `/` 時回首頁。
- 長按返回：從任意層級直接回首頁。
- 返回上一層後選中剛離開的資料夾。
- 字型掃描同時支援 `/font` 與 `/fonts`，並改善中文／空格／符號的外部 BIN 檔名解析。

### 穩定性

- 停用 Paper S3 閒置時降到 80 MHz，避免觸控、SD、圖片解碼及背景工作在切頻後偶發當機。
- Wi-Fi 設定畫面離開後會正確停止掃描／連線並關閉 Wi-Fi（網路服務父 Activity 仍可維持連線）。
- 補強輸入事件、render busy、pending 翻頁、display idle 與記憶體診斷 LOG。

## 升級注意事項

- 方向感應功能首次使用前必須重新完成四姿勢校正。
- 段首縮排與部分閱讀快取格式已更新；舊快取不相容時會自動重建。若頁面仍異常，可從閱讀選單刪除該書快取。
- 漫畫檔建議使用零補齊檔名（例如 `001.jpg`、`002.jpg`），雖然韌體也會進行自然排序。
- Wi-Fi 上傳、OTA 與 KOReader Sync 保留既有架構，仍建議視為進階功能並在正式使用前自行驗證。

## 建置

```sh
pio run -e default
pio run -e gh_release
```

發布 tag 建議使用：

```text
v1.8.4
```

GitHub Actions 會建立韌體、合規 source package、LGPL relink kit、license bundle、SBOM 與 SHA256SUMS。
